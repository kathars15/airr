# Manual Control Helpers

Scripts that drive the running application or send simple control commands.

Scripts:

- `auto_calibrate.py`: run `main2.py` and feed interactive calibration commands automatically.
- `control.py`: small control-command helper using saved track logs.

These scripts may start the main program or send commands, so use them only when the radar/optical runtime is expected to be active.
